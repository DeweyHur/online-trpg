# Supabase Configuration
# Copy this file to .env and replace with your actual Supabase project credentials
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Authentication Configuration (Optional)
# These are used for anonymous user authentication
AUTH_GUEST_EMAIL=guest@trpg.local
AUTH_GUEST_PASSWORD=guest123456

# Note: The actual .env file should not be committed to version control
# Add .env to your .gitignore file 

# External Text-to-Speech (TTS) Configuration
# To use Azure Speech free tier, set these values in your local .env
# TTS_PROVIDER can be 'azure'
TTS_PROVIDER=
AZURE_SPEECH_REGION=
AZURE_SPEECH_KEY=
AZURE_DEFAULT_VOICE=en-US-JennyNeural